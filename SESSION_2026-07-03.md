# 密云水库项目 — 开发记录 2026-07-03

## 今日完成内容

### 一、3D 场景检测全链路修复

#### 问题 1：超时 60s → "未检测到目标"
**根因：** `server.js` AbortController 超时设置为 60s，qwen3.7-plus 视觉推理耗时超过 60s。

**修复：**
- 超时从 60s 延长至 **150s**
- 截图分辨率从 1280px 降至 **800px**，减少传输和推理时间
- 请求质量从 0.9 降至 0.85

#### 问题 2：`PickWorldPointByScreenPos` 返回场景本地坐标
**根因：** 返回格式为 `{ point: [x,y,z], eid: "" }`，是场景笛卡尔坐标，不是经纬度。

**修复：** 调用 `App.Tools.Coordinate.CartesianToGIS([cartPoint])` 转换，返回格式为 `{ to: [[lng, lat, alt]] }`。

```js
const cartPoint = Array.isArray(r.result) ? r.result : r.result?.point
const gisRes = await App.Tools.Coordinate.CartesianToGIS([cartPoint])
const [lng, lat] = gisRes.result?.to?.[0]
```

#### 问题 3：标记偏向右边
**根因：** 坐标映射用了 player 的 CSS 尺寸（~1920px），但 `PickWorldPointByScreenPos` 期望截图坐标空间（800px），导致 2.4× 系数造成系统性右偏。

**修复：** 改用 `scene.width / scene.height` 做坐标映射：

```js
const sx = Math.round((x1 + x2) / 2 / 1000 * scene.width)   // 800px
const sy = Math.round((y1 + y2) / 2 / 1000 * scene.height)  // 378px
```

#### 问题 4：思维链拖慢速度
**修复：** `server.js` 请求体加 `enable_thinking: false`，禁用 qwen3.7-plus 的推理过程；同时剥离 `<think>...</think>` 块再解析 JSON（双重保险）。

#### 问题 5：检测描述用英文、无标记图标
**修复：**
- prompt 改为中文描述（≤8字）
- POI 加红色定位针 SVG 图标（`markerNormalUrl` + `markerVisible: true`）

---

### 二、AI 地图对话支持 3D 场景截图

**根因：** 地图分析 Tab 的 AI 对话调用 `map-agent/api/chat`，map-agent 用 Playwright 自行截 2D AMap，与 3D 模式无关。

**修复：**

1. `map-agent/main.py` — `ChatRequest` 新增字段：
   ```python
   screenshot_b64: str = ""     # 外部截图（3D 场景等）
   scene_type: str = "satellite"
   ```
   有 `screenshot_b64` 时跳过 Playwright，直接用传入截图。

2. `map-agent/agent.py` — `chat_with_map` 新增 `scene_type` 参数，3D 时切换系统提示为三维场景描述；图片 MIME 自动检测（JPEG/PNG）。

3. `src/_mapAnalysisTab.js` — `doChat()` 在 3D 模式下先调 `_capture3DScene()` 获取截图，随请求传入。

---

### 三、水位线行为调整

**用户要求：**
- 打开地图分析 Tab 时不要预加载水位线
- 勾选水位线时不要自动切换到卫星图
- 水位线只在用户手动勾选时按需加载

**修复：**
- 移除 `_initMapFeatures` 中 `WL_LEVELS.forEach(lv => _ensureWL(lv.name))` 预加载
- 移除 1.2s 后自动勾选 160m 水位线的 `setTimeout`
- 移除 `_toggleWL` 中的 `setMapMode('satellite')` 自动切图
- 加 `_map` 空值守护 + 错误原因显示在 `title` 属性

---

### 四、`_aiChat.js` 3D 截图修复

**修复：** 改用 `App.Renderer.GetSnapshot` 直接调用（去掉 optional chaining 导致的静默失败）；自动检测 PNG/JPEG MIME 类型并传入 `screenshotData.mime`。

---

## 服务启动方式

```bash
# 前端（端口 5173 或 5174）
cd D:/miyunshuiku && npm run dev

# 后端 Express（端口 3001）
cd D:/miyunshuiku && node server.js

# map-agent Python（端口 8000）
PYTHON="/c/Users/张轩/AppData/Local/Programs/Python/Python313/python.exe"
cd /d/miyunshuiku/map-agent && "$PYTHON" main.py

# 访问地址
http://localhost:5173
```

---

## 明日待做 / 可继续方向

### 高优先级
- [ ] **验证 3D 检测完整流程**：截图 → AI 检测（中文描述）→ 坐标转换 → POI 标注（红色图标）→ 位置是否准确
- [ ] **验证地图 AI 对话 3D 截图**：在 3D 场景下提问，AI 应能描述 3D 场景内容，不再分析 2D 地图
- [ ] **水位线勾选流程**：切换到卫星图 → 勾选水位线 → 确认正常加载显示

### 中优先级
- [ ] **3D 标注精度优化**：截图 800×378 映射到 `PickWorldPointByScreenPos` 坐标是否真正对应 WDP 渲染分辨率？如有偏差，需进一步校正
- [ ] **检测置信度过滤**：低置信度（< 0.5）的检测结果是否需要过滤
- [ ] **标注 label 背景色**：当前白色文字+透明背景，在浅色场景下可读性差

### 低优先级
- [ ] **2D 检测后切回 3D** 时，AMap 标注是否在 3D 叠加层上可见
- [ ] 清理旧 Vite 进程，统一用 5173

---

## 关键文件索引

```
D:/miyunshuiku/
├── src/
│   ├── _mapAnalysisTab.js   ★ 主要改动，坐标修复 / POI 图标 / doChat 3D 截图 / 水位线逻辑
│   └── _aiChat.js           ★ 3D 截图 MIME 自动检测
├── server.js                ★ 思维链禁用 / 中文 prompt / 超时 150s / think 块剥离
└── map-agent/
    ├── main.py              ★ ChatRequest 加 screenshot_b64/scene_type
    └── agent.py             ★ chat_with_map 加 scene_type 参数 / MIME 自动检测
```

---

## 注意事项

- **`CartesianToGIS` 返回格式**：`{ success: true, result: { to: [[lng, lat, alt]] } }`，用 `result.to[0]`，不是 `result[0]`
- **截图坐标空间**：`GetSnapshot([800, 378])` 截图后，`PickWorldPointByScreenPos` 期望的是 **800×378** 的坐标，不是 player 的 CSS 尺寸
- **`enable_thinking: false`** 只在请求体里加字段，DashScope 若不支持会自动忽略；剥离 `<think>` 块是保底措施
- **水位线 GeoJSON 格式**：单 `Polygon` 要素，`coordinates[0]` 是外环，`_loadWLLayer` 循环遍历所有环是为了兼容带洞的多边形
