# 密云水库项目 — 开发记录 2026-07-02

## 今日完成内容

### 一、map-agent 前端迁移（无 iframe 化）

将 `map-agent/templates/index.html` 的所有前端逻辑迁移为原生 ES 模块，彻底废弃 iframe 方案。

**修改文件：**

| 文件 | 改动摘要 |
|------|----------|
| `src/_amap.js` | 新增 `export function getMapInstance()` 导出共享 AMap 实例 |
| `src/_mapAnalysisTab.js` | 完全重写，~1843 行，迁移所有 map-agent 前端逻辑 |

**迁移的功能模块（全部复用主项目 AMap 实例）：**
- 目标检测（单次 + SSE 流式标记/矩形框）
- 区域遍历分析（框选 + 进度条 + 摘要弹窗）
- 专业模式 AI Earth 提取（建筑/路网/拦河坝/自定义）
- 多轮 AI 地图对话球（`position: fixed` 右下角）
- 水位线图层（6 条：155~160m，独立开关，跨 tab 保持）
- 精准测量（距离/面积，MouseTool）
- 地图搜索定位（AutoComplete + 历史记录）
- 视口信息（缩放/瓦片数/四角坐标）

**关键设计：**
- 所有 HTML 元素 ID 加 `ma-` 前缀防冲突
- 侧边栏注入到 `.side-panel--left`，用 `top/left/right/bottom: -10px` 抵消父容器 10px padding
- show 时给父容器加 `overflow: hidden`，hide 时还原

---

### 二、UI 布局调整

**左侧面板（`#ma-sidebar`）保留：**
- 🔍 搜索定位
- 分析目标 + 启动/停止/清除标注
- 视口信息
- 精准测量
- 视野分析
- 状态栏 + 运行日志 + 截图预览

**右侧面板（`#ma-pro-panel`）保留（从上到下）：**
- 专业模式（AI Earth 提取范围/类型/操作）
- 运行日志
- 🗺️ 区域遍历分析（从左侧移过来）
- 💧 水位线图层（从左侧移过来，位于 AI 对话按钮上方）

---

### 三、Bug 修复

#### Bug 1：3D 模式下进入地图分析 Tab 失败
**根因：** `_map.getBounds()` 在 AMap 容器 `display:none` 时返回 null，`bounds.getNorthEast()` 在 try/catch 外抛错。

**修复：**
- `showMapAnalysisTab()` 检测到 3D 模式后先调 `await setMapMode('satellite')` 再初始化地图功能
- `_startAnalysis()` 加 bounds 空值守护，graceful 提示用户切换地图

```js
// src/_mapAnalysisTab.js — showMapAnalysisTab
if (getMapMode() === '3d') {
  setMapMode('satellite').then(() => _initMapFeatures())
} else {
  _initMapFeatures()
}
```

#### Bug 2：AI 助手 DEM 分析报"缺少依赖"
**根因：** Python 环境缺少 `scipy`，`query_elevation.py` import 报 `ModuleNotFoundError`。

**修复：**
```bash
# 使用清华镜像安装
py -m pip install scipy -i https://pypi.tuna.tsinghua.edu.cn/simple
# scipy 1.18.0 已安装
```

另发现：`server.js` 用 `'python'` 命令但 Node.js 环境中只有 `py` 可用（Windows Launcher）。

```js
// server.js — _execDemTool 和 /api/dem/kml 两处
const python = process.platform === 'win32' ? 'py' : 'python3'
```

#### Bug 3：水位线在卫星图加载不了
**根因：** 与 Bug 1 同源——3D 模式下 `_map.add(polyline)` 对隐藏容器无效，切换后不渲染。修复 3D 模式自动切换后，水位线随 Bug 1 同步修复。

#### Bug 4：分析完成后无法清除标注
**修复：** 分析 `done` 事件后显示「✕ 清除标注」按钮，点击移除所有 markers/overlays，启动新分析时自动隐藏。

---

### 四、服务启动方式

> **注意：** 5173 端口被旧进程占用，Vite 自动使用 5174。

```bash
# 前端（已在 5174 运行）
cd D:/miyunshuiku && npm run dev

# 后端 Express（已在 3001 运行）
cd D:/miyunshuiku && node server.js

# map-agent Python（已在 8000 运行）
PYTHON="/c/Users/张轩/AppData/Local/Programs/Python/Python313/python.exe"
cd /d/miyunshuiku/map-agent && nohup "$PYTHON" main.py &

# 访问地址
http://localhost:5174
```

---

## 明日待做 / 可继续方向

### 高优先级
- [ ] **测试 DEM 分析全链路**：AI 助手 → 工具调用 → `query_elevation.py` → 标注渲染到 3D 场景，scipy 刚装好，需端到端验证
- [ ] **测试水位线 + 3D 切换流程**：从 3D 进入地图分析 → 自动切换卫星 → 水位线加载 → 切回 3D 是否正常
- [ ] **测试区域遍历（右侧新位置）**：框选 → 开始遍历 → 进度条 → 报告弹窗

### 中优先级
- [ ] **地图分析 Tab 的搜索栏**：CLAUDE.md 提到"2D 模式下不显示搜索栏"，需确认地图分析 Tab 里的 `#ma-search-input` 是否与主项目搜索栏冲突
- [ ] **遍历弹窗拖拽**：移到右侧面板后，拖拽逻辑坐标系是否还正确
- [ ] **水位线首次加载优化**：6 条水位线同时预加载，弱网下可能慢，考虑懒加载

### 低优先级
- [ ] 清理旧 Vite 进程（占用 5173），统一用 5173
- [ ] `_mapEventsRegistered` 变量位置（在使用它的函数之后声明），可移到文件顶部模块状态区

---

## 关键文件索引

```
D:/miyunshuiku/
├── src/
│   ├── _mapAnalysisTab.js   ★ 主要改动，1843 行，map-agent 前端全量迁移
│   ├── _amap.js             ★ 新增 getMapInstance() 导出
│   ├── _tabSwitch.js        调用 showMapAnalysisTab / hideMapAnalysisTab（未改）
│   └── main.js              bootstrap：initAmap(128) → onSceneReady(initMapAnalysisTab)(134)
├── server.js                ★ python → py，DEM 工具两处修复
├── query_elevation.py       DEM 分析脚本（依赖 scipy/tifffile/numpy）
└── map-agent/
    └── main.py              Python FastAPI，端口 8000，未改动
```

---

## 注意事项

- **3D/2D 切换逻辑**：`_amap.js` 中 `SYNC_YAW = -90` 是对齐 2D 地图正北的关键参数，勿随意修改
- **水位线缓存**：`_wlCache` 在模块生命周期内持久化，刷新页面才清空；切换 tab 不清除水位线是设计行为
- **DEM 分析超时**：`execSync` 设置 `timeout: 120000`（2 分钟），大范围分析可能接近上限
- **map-agent 依赖 Playwright Chromium**：截图功能依赖 `playwright install chromium`，服务器部署时需额外安装
