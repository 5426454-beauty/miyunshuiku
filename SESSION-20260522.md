# 工作记录 — 2026-05-22

**项目**：借用同事场景移植三项功能到漳河水库管理矩阵项目
**新工程目录**：`C:\shuikujuzhen-borrow-scene\`
**原工程目录**：`C:\shuikujuzhen\`（保持原样未动）

---

## 执行概要

按照 `HANDOFF.md` 的 9 步任务清单，完成了全部工作：

| # | 任务 | 状态 |
|---|---|---|
| 1 | 创建回退备份 zip | ✅ |
| 2 | 复制项目到新目录 | ✅ |
| 3 | 切换 order 到同事工程 | ✅ |
| 4 | 移植闸门启闭模块 | ✅ |
| 5 | 移植降雨水位模块 | ✅ |
| 6 | 移植无人机巡检模块 | ✅ |
| 7 | 挂载四管 Tab UI 入口 | ✅ |
| 8 | 扩展 AI 助手指令 | ✅ |
| 9 | 构建并验证 | ✅ |
| 10 | 坐标适配到广州场景 | ✅（额外追加） |

---

## 关键决策

- **备份位置**：C 盘根目录受 UAC 保护，备份 zip 改为 `C:\Users\51world\Desktop\shuikujuzhen-backup-20260521.zip`（370KB）
- **复制策略**：robocopy 排除 `node_modules`、`dist`、`.wdp-cache`、`AI demo`、`*.docx`、`AI demo.rar`
- **order 切换**：从漳河水库 `5370da1dcb6547214dca7a5a1282a568` → 铜锣湾水库 `81a65f148cdf78a73ee795e30826ac7e`

---

## 新建文件

| 文件 | 说明 |
|---|---|
| `src/_gate.js` | 闸门启闭 + 水花联动，三闸门独立控制，高度可调 |
| `src/_rainfall.js` | 降雨量→水位联动，滑块控制水面 Z 抬升，自动切换天气 |
| `src/_drone.js` | 无人机巡检：App.Bound 路径飞行 + 相机 Follow + 安全解锁 |

### 各模块导出接口

**_gate.js**
- `toggleGate(idx)` — 切换闸门开闭（idx: 0/1/2 对应 1/2/3 号）
- `setGateHeight(m)` — 设置开启高度（1-30m）
- `stopAllGates()` — 强制停止所有闸门并复位
- `initGate()` — 创建悬浮球 UI
- `initGates()` — 场景就绪后隐藏水花
- `destroyGate()` — 停止并清理

**_rainfall.js**
- `startRainfall(mm)` — 启动降雨模拟（mm: 0-300）
- `resetWaterLevel()` — 复位水面高度
- `initRainfall()` — 创建悬浮球 UI + 面板
- `initRainfallScene()` — 获取水面实体引用
- `destroyRainfall()` — 停止动画并复位

**_drone.js**
- `toggleDrone()` — 切换无人机巡检
- `startDrone()` — 启动飞行 + 相机跟随
- `stopDrone()` — 安全停止（Stop() 解锁 → 清理资源 → 隐藏 → 恢复相机）
- `initDrone()` — 创建悬浮球 UI
- `destroyDrone()` — 停止并清理

---

## 修改文件

| 文件 | 变更 |
|---|---|
| `config.json` | order → `81a65f148cdf78a73ee795e30826ac7e` |
| `src/_wdp.js` | `WDP_CONFIG_DEFAULT.order` → 同事工程 |
| `src/_tabSwitch.js` | 导入新模块；离开四管时 stopAllGates/destroyRainfall/destroyDrone；进入四管时 initGates/initRainfallScene；`_setToolsVisible` 新增 3 个选择器 |
| `src/_aiChat.js` | 新增导入 + 4 条系统指令 + 4 个 case 处理器：`toggleGate`、`setRainfall`、`resetRainfall`、`toggleDrone`；场景描述更新为广州 |
| `src/style.css` | 新增 `.gate-tool`、`.rainfall-tool`、`.drone-tool` 三组完整样式（悬浮球 + 面板），各 70-130 行 |
| `src/main.js` | `_bootstrap()` 中新增 `initGate()`、`initRainfall()`、`initDrone()` 调用 |
| `src/_camera.js` | 复位位置 → `[113.4373, 23.3053, 500]` |
| `src/_heatmap.js` | 热力散布点 + FlyTo 目标 → 广州中心 |
| `src/_migration.js` | 6 条飞线 + FlyTo 目标 → 广州中心 |
| `src/_inspection.js` | 3 条路线 + 观察相机 → 围绕无人机巡检区域 |
| `src/_surveillance.js` | 4 个监控 POI + 周界 polygon → 广州区域 |
| `MEMORY.md` | 场景中心坐标 + 相机复位坐标更新 |

---

## 场景资产 EID（同事工程）

```js
// 闸门 + 水花
GATE_CONFIGS = [
  { label: '1号', gateEid: "842689664719519744", waterEid: "844158235551203328" },
  { label: '2号', gateEid: "837014958498643968", waterEid: "837021086729109504" },
  { label: '3号', gateEid: "842689118667276288", waterEid: "844158079737004032" }
]

// 水面
WATER_SURFACE_EID = "836996280512151552"

// 无人机（Skeletal 骨骼动画）
DRONE_EID = "840945068910051328"

// 无人机巡检路径
PATH_COORDINATES = [
  [113.437751, 23.306834, 100],
  [113.441966, 23.304840, 100],
  [113.437426, 23.304002, 100],
  [113.434274, 23.305188, 100],
  [113.437751, 23.306834, 100],
]
```

---

## 四管 Tab 悬浮球布局（scene-container 内）

| 位置 | 选择器 | 图标 | 功能 |
|---|---|---|---|
| left:292px, bottom:80px | `.heatmap-tool` | 🔥 | 场景热力 |
| left:348px, bottom:80px | `.migration-tool` | 🌐 | 迁徙图 |
| left:404px, bottom:80px | `.viewshed-tool` | 👁 | 可视域 |
| left:460px, bottom:80px | `.gate-tool` | 🚪 | 闸门控制 |
| left:516px, bottom:80px | `.rainfall-tool` | 🌧 | 降雨模拟 |
| left:572px, bottom:80px | `.drone-tool` | 文字按钮 | 无人机巡检 |

- 默认 `display: none`，切换到四管时 `_setToolsVisible(true)` 显示
- 离开四管时自动销毁场景实体并隐藏

---

## 已知注意事项

1. 闸门停止时必须强制复位：`s.entity.location = [...s.origin]`
2. 水花初始隐藏，只在闸门启动时 `bVisible: true`
3. `Delete()` 必须 `await`
4. 相机跟随必须用 `App.CameraControl.Stop()` 解锁，`Focus()` 不能覆盖 `Follow` 锁
5. `Focus.entity` 必须传数组：`entity: [obj]`，不是 `entity: obj`
6. SDK 调用每步独立 try-catch
7. `RegisterSceneEvent`（单数）用于渲染器事件，`RegisterSceneEvents`（复数）用于交互事件
8. 禁用 API：`App.Camera.FlyTo()` / `App.Scene.SetWeather()` / `App.Interaction.PickPoint()` / `StopFollow()`

---

## 回退方案

1. 删除 `C:\shuikujuzhen-borrow-scene\` 即回退
2. 或恢复备份：`C:\Users\51world\Desktop\shuikujuzhen-backup-20260521.zip`
3. 或切回原 order：`config.json` 的 order 改回 `5370da1dcb6547214dca7a5a1282a568`

---

## 构建验证

```bash
cd C:\shuikujuzhen-borrow-scene
npm install    # 235 packages
npx vite build # 3573 modules, 8.8s, ✓ success
```

**Dev server**：`http://localhost:5173`（Vite）+ `http://localhost:3001`（Express API）

---

## 闸门控制 Bug 修复记录（2026-05-22 续）

### Bug #1：闸门打开后自动关闭

**现象**：打开闸门后，不等用户点击停止，5 秒后自动关闭。

**根因**：`runGate` 函数中有一层 `while (s.active && !s.abort)` 外循环（来自同事 demo 的自动演示逻辑），每次循环都执行“上升 → 等 5 秒 → 下降 → 等 5 秒”。

**修复**：去掉外循环，改为：
1. 上升到目标高度
2. `while (!s.abort) { await wait(200) }` 保持开启，轮询等待停止信号
3. 收到信号后执行下降动画 + 复位

**涉及文件**：`src/_gate.js`（`runGate` 函数）

---

### Bug #2：闸门关闭无过渡动画，瞬间归位

**现象**：点击停止后闸门瞬间跳到原位，没有下降过渡过程。

**根因（第一次定位错误）**：以为是 `toggleGate` 的 else 分支直接 `s.entity.location = [...s.origin]` 跳过动画。修复为等待 `runGate` 完成后才归位，但实际测试仍无效。

**根因（第二次定位 — 真正原因）**：
`runGate` 内部的下降循环条件为 `while (!s.abort && z > OZ)`，但此时 `s.abort` 已被 `toggleGate` 设为 `true`，`!s.abort` 为 `false`，循环体一次都不执行，直接跳到最后的 `location = [...s.origin]` 瞬间归位。

**修复**：在保持循环退出后、进入下降动画前，插入 `s.abort = false` 重置标志位，让下降循环能正常执行。

```javascript
// 保持开启状态，等待用户点击停止
while (!s.abort) {
  await wait(200)
}

// ← 修复关键：重置 abort 标志，让下降动画能执行
s.abort = false

// 下降复位（此时 !s.abort 为 true，循环正常执行）
z = s.entity.GetData().location[2]
while (!s.abort && z > OZ) {
  z -= STEP
  s.entity.location = [OX, OY, z]
  syncWater()
  await wait(SPEED)
}
```

**涉及文件**：`src/_gate.js`（`runGate` + `toggleGate` 函数）

---

## 相机复位 + 无人机结束视角更新（2026-05-22 续）

将复位视角从估算值替换为场景中实测的真实位姿，并统一无人机停止后的相机恢复目标。

### 实测复位视角

```
location: [113.43707149070427, 23.305665621168774, 101.59671183589296]
rotation: { pitch: -18.150018692016602, yaw: 17.967817306518555 }
flyTime: 2
```

### 修改内容

| 文件 | 变更 |
|---|---|
| `src/_camera.js` | `RESET_POSE` 替换为实测位姿 |
| `src/_drone.js` | `stopDrone()` 第四步由 `Focus(gateEid)` 改为 `SetCameraPose(RESET_POSE)` |

无人机巡检停止后镜头不再飞到闸门位置，而是与复位按钮一致，飞回全景俯视视角。